## CX as Code v1.75.0

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.75.0)
### Resources added
- `genesyscloud_speechandtextanalytics_dictionaryfeedback`
- `genesyscloud_workforcemanagement_businessunits`

### Resources changed

#### genesyscloud_quality_forms_evaluation

Added:
- `question_groups.questions.type` — question type; valid values are `multipleChoiceQuestion`, `multipleSelectQuestion`, `freeTextQuestion`, `npsQuestion`, and `readOnlyTextBlockQuestion`.
- `question_groups.questions.multiple_select_option_questions` — nested questions for multiple-select question types.
- `question_groups.questions.multiple_select_option_questions.text` — label for a multiple-select option.
- `question_groups.questions.multiple_select_option_questions.help_text` — help text for the option.
- `question_groups.questions.multiple_select_option_questions.type` — option question type.
- `question_groups.questions.multiple_select_option_questions.na_enabled` — enable a not-applicable answer for the option.
- `question_groups.questions.multiple_select_option_questions.comments_required` — require comments for the option.
- `question_groups.questions.multiple_select_option_questions.visibility_condition` — visibility rules for the option.
- `question_groups.questions.multiple_select_option_questions.answer_options` — answer choices for option questions that use multiple choice.
- `question_groups.questions.multiple_select_option_questions.is_kill` — mark the option as a fatal question.
- `question_groups.questions.multiple_select_option_questions.is_critical` — mark the option as a critical question.
- `question_groups.questions.answer_options.built_in_type` — built-in answer type for multiple-select options; valid values are `Selected` and `Unselected`.
- `question_groups.questions.answer_options.assistance_conditions` — conditions combined with logical AND for answer assistance.
- `question_groups.questions.answer_options.assistance_conditions.operator` — assistance operator; valid values are `EXISTS` and `NOTEXISTS`.
- `question_groups.questions.answer_options.assistance_conditions.topic_ids` — topic IDs combined with logical OR.

Removed:
- None detected.

Changed:
- `question_groups.questions.answer_options` — no longer always required; required for `multipleChoiceQuestion` type questions.
- `question_groups.questions.answer_options.text` — no longer always required; optional when using built-in answer types.

#### genesyscloud_webdeployments_configuration

Added:
- `journey_events.tracking_settings` — tracking behavior and filtering configuration.
- `journey_events.tracking_settings.should_keep_url_fragment` — keep the URL fragment in tracking; defaults to `false`.
- `journey_events.tracking_settings.search_query_parameters` — query parameters used for search tracking.
- `journey_events.tracking_settings.excluded_query_parameters` — query parameters excluded from the query string.
- `journey_events.tracking_settings.ip_filters` — IP address or CIDR filters for tracking restrictions.
- `journey_events.tracking_settings.ip_filters.ip_address` — IP address or CIDR range to filter.
- `journey_events.tracking_settings.ip_filters.name` — descriptive name for the filter.

Removed:
- None detected.

Changed:
- None detected.

#### genesyscloud_telephony_providers_edges_extension_pool

Added:
- `division_id` — division for the extension pool; optional on create/update and populated from the API on read.

Removed:
- None detected.

Changed:
- None detected.

#### genesyscloud_oauth_client

Added:
- None detected.

Removed:
- None detected.

Changed:
- `client_secret` — no longer written to Terraform state on read, create, or update.

#### genesyscloud_auth_division

Added:
- None detected.

Removed:
- None detected.

Changed:
- Delete behavior — destroy succeeds when the division is already absent from the org (HTTP 404).

#### genesyscloud_script

Added:
- None detected.

Removed:
- None detected.

Changed:
- Read behavior — removes the resource from state when the script was deleted outside Terraform.

#### genesyscloud_team

Added:
- None detected.

Removed:
- None detected.

Changed:
- Member management — when adding members partially fails, state is synced with successfully added members and apply must be re-run to retry failures.
- Create and update behavior — on member update failure, the provider re-reads the team to refresh state before returning errors.

#### genesyscloud_location

Added:
- None detected.

Removed:
- None detected.

Changed:
- `address` — updates omit the address from API requests when an emergency number is assigned unless the address itself changed, avoiding update errors when changing other fields.

#### genesyscloud_tf_export

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — routing queue exports resolve `conditional_group_activation.rules.groups.member_group_id` references.

### Resources removed
None detected.

### Data sources added
- `genesyscloud_speechandtextanalytics_dictionaryfeedback`
- `genesyscloud_workforcemanagement_businessunits`

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Two new resources and matching data sources are available: `genesyscloud_speechandtextanalytics_dictionaryfeedback` and `genesyscloud_workforcemanagement_businessunits`.
- `genesyscloud_quality_forms_evaluation` supports additional question types and multiple-select question structures; existing forms that relied on always-required `answer_options` may need configuration updates.
- `genesyscloud_webdeployments_configuration` can configure journey tracking filters and URL/query behavior under `journey_events.tracking_settings`.
- `genesyscloud_telephony_providers_edges_extension_pool` accepts an optional `division_id`.
- `genesyscloud_oauth_client` no longer stores `client_secret` in state; do not rely on Terraform state to retrieve secrets after upgrade.
- `genesyscloud_auth_division` destroy no longer fails when the division was already deleted manually.
- `genesyscloud_script` drops removed scripts from state on refresh instead of leaving a stale entry.
- `genesyscloud_team` may show updated `member_ids` after partial member-add failures and requires a follow-up apply to retry failed members.
- `genesyscloud_location` updates are less likely to fail when a location has an emergency number and you change non-address fields.
- `genesyscloud_tf_export` routing queue exports handle member group references inside conditional group activation rules.
- Flow file download during architect flow operations is more resilient to API rate limits; no schema changes are required.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.75.0/docs) before making upgrade decisions._
