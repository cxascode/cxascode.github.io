## CX as Code v1.77.3

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.3)

### Resources added
None detected.

### Resources changed

#### genesyscloud_integration_credential

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — `name` values prefixed with `Integration-` are now exported as Terraform references to `genesyscloud_integration` resources instead of raw prefixed strings.

#### genesyscloud_integration_custom_auth_action

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — `parent_integration_id` is now resolved as a reference to `genesyscloud_integration`, using `integration_id` when needed during export.

#### genesyscloud_webdeployments_configuration

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — `messenger.apps.knowledge.knowledge_base_id` is now resolved as a reference to `genesyscloud_knowledge_knowledgebase` (replacing the prior `apps.knowledge.knowlege_base_id` path).
- Export behavior — `support_center.enabled_categories.category_id` is now resolved as a reference to `genesyscloud_knowledge_category` (replacing the prior `enabled_categories.category_id` path).
- Export behavior — `support_center.knowledge_base_id` is now resolved as a reference to `genesyscloud_knowledge_knowledgebase`.
- Export behavior — `support_center.label_filter.label_ids` is now resolved as references to `genesyscloud_knowledge_label`.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Users exporting `genesyscloud_integration_credential`, `genesyscloud_integration_custom_auth_action`, or `genesyscloud_webdeployments_configuration` may see improved cross-resource references in generated HCL.
- Existing Terraform configurations for plan, apply, and import are likely unaffected; changes apply to export output only.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.3/docs) before making upgrade decisions._
