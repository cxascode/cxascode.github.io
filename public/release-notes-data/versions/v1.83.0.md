## CX as Code v1.83.0

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.83.0)

### Resources added
None detected.

### Resources changed
None detected.
### Runtime behavior changes

#### Plan behavior
None detected.

#### Apply behavior
None detected.

#### Export behavior
- `genesyscloud_ai_studio_summary_setting` — Export enumeration now passes page number, page size, and name filter to the list API in the correct argument positions, so exported summary settings are complete and correctly filtered.
- `genesyscloud_business_rules_decision_table` — Queue ID reference resolution during export now matches row input and output literal values using exact attribute paths (`rows.inputs.literal.value` and `rows.outputs.literal.value`) instead of wildcard paths that did not match.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
- `aws_region` — Accepts `eusc-de-east-1` for EU Sovereign Cloud (Germany) orgs, routing API calls to `edee1.eusc-pure.cloud`.

### Upgrade impact
Existing configurations are likely unaffected unless you manage orgs in the EU Sovereign Cloud Germany region, export `genesyscloud_ai_studio_summary_setting` resources, or export `genesyscloud_business_rules_decision_table` resources that reference routing queues in row literal values. Set `aws_region = "eusc-de-east-1"` when managing EU Sovereign Cloud orgs in that region. No new resources, removed resources, or schema attribute changes were detected in this release.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.83.0/docs) before making upgrade decisions._
