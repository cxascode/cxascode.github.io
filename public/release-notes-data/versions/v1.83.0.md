## CX as Code v1.83.0

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.83.0)

### Resources added
None detected.

### Resources changed

#### genesyscloud_ai_studio_summary_setting

Added:
- None detected.

Removed:
- None detected.

Changed:
- List and read behavior — Pagination and name filtering for summary settings now use the correct API parameter order, so list, refresh, and export operations return the expected results.

#### genesyscloud_business_rules_decision_table

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — Queue IDs referenced in `rows.inputs.literal.value` and `rows.outputs.literal.value` are now resolved during export; previous wildcard resolver paths did not match the exporter framework and were skipped silently.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
- `aws_region` — Accepts `eusc-de-east-1` for EU Sovereign Cloud (Germany) orgs, routing API calls to `edee1.eusc-pure.cloud`.

### Upgrade impact
- Existing configurations are likely unaffected unless you deploy to the EU Sovereign Cloud region or export decision tables with queue references in row literal values.
- Set `aws_region = "eusc-de-east-1"` in provider configuration when managing EU Sovereign Cloud orgs in that region.
- Re-export `genesyscloud_business_rules_decision_table` resources if row literals contain queue IDs that previously exported as unresolved IDs.
- No new resources, removed resources, or schema attribute changes were detected in this release.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.83.0/docs) before making upgrade decisions._
