## CX as Code v1.60.0

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.60.0)

### Resources added
None detected.

### Resources changed

#### genesyscloud_group

Added:
- `calls_enabled` — optional boolean (defaults to `true`) that controls whether calls can be placed to the group.

Removed:
- None detected.

Changed:
- None detected.

#### genesyscloud_routing_queue

Added:
- None detected.

Removed:
- None detected.

Changed:
- `media_settings_message` — message media settings are now mapped with message-specific read and write logic, including `sub_type_settings` and service-level fields, instead of the generic media settings handler.

#### genesyscloud_routing_email_route

Added:
- None detected.

Removed:
- None detected.

Changed:
- Read/refresh — retries when the API returns empty or missing route lists instead of clearing the resource from state; a route that is not found in its domain triggers a retryable error rather than silently removing the resource.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
- `token_acquire_timeout` — optional duration for waiting on an available OAuth client from the token pool (default `5m`).
- `token_init_timeout` — optional duration for initializing the token pool at provider startup (default `10m`).
- `sdk_client_pool_debug` — optional boolean that enables debug logging for token pool operations.

### Upgrade impact
- `genesyscloud_group` gains the optional `calls_enabled` argument (default `true`). Existing group configurations should continue to work without changes.
- `genesyscloud_routing_queue` resources that use `media_settings_message` may show plan diffs after upgrade because state is mapped more accurately to the API.
- `genesyscloud_routing_email_route` refresh behavior is more robust when the API returns empty or missing route lists; resources are no longer silently dropped from state when a route is temporarily unavailable.
- Exporting `genesyscloud_integration_credential` resources now generates block labels in the form `Integration-{integration_name}` instead of `{integration_name}_{credential_name}`.
- Exporting `genesyscloud_integration_action` resources now preserves zero-value entries in `config_response.translation_map_defaults`.
- Bulk resource export limits concurrent API operations to the configured `token_pool_size`, which can affect large export runs.
- Plan and apply operations that exhaust the token pool may now fail with a timeout after `token_acquire_timeout` instead of blocking indefinitely.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.60.0/docs) before making upgrade decisions._
