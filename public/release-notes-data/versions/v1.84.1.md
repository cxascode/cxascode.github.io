## CX as Code v1.84.1

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.84.1)

### Resources added
None detected.

### Resources changed

#### genesyscloud_routing_email_domain

Added:
- None detected.

Removed:
- None detected.

Changed:
- Create behavior — If create returns a 400 error indicating the domain already exists (an eventual-consistency race in some regions), the provider adopts the existing domain into Terraform state instead of failing apply. When `mail_from_domain`, `custom_smtp_server_id`, `graph_api_settings`, or `imap_settings` differ from your configuration, it updates the adopted domain; otherwise it refreshes state from the API.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing configurations are likely unaffected unless you create `genesyscloud_routing_email_domain` resources in regions where the API can create the domain but still return a "domain already exists" error.
- Apply should succeed in that race-condition scenario instead of failing; the provider adopts the existing domain and continues with update or read as needed.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.84.1/docs) before making upgrade decisions._
