## CX as Code v1.77.4

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.4)

### Resources added
None detected.

### Resources changed

#### genesyscloud_outbound_messagingcampaign

Added:
- `whats_app_config` — Optional block to configure WhatsApp messaging for the campaign; exactly one of `email_config`, `sms_config`, or `whats_app_config` must be set.
- `whats_app_config.whats_app_columns` — Contact list columns that specify the WhatsApp address(es) for each contact.
- `whats_app_config.whats_app_integration_id` — WhatsApp integration used to send messages to contacts.
- `whats_app_config.content_template_id` — Content template used to formulate the WhatsApp message sent to contacts.

Removed:
- None detected.

Changed:
- `email_config` — Validation now requires exactly one of `email_config`, `sms_config`, or `whats_app_config`.
- `sms_config` — Validation now requires exactly one of `email_config`, `sms_config`, or `whats_app_config`.

### Runtime behavior changes

#### Plan behavior
- `genesyscloud_outbound_messagingcampaign` — `whats_app_config` is populated from the Genesys Cloud API during plan refresh for campaigns configured with WhatsApp.
- `genesyscloud_knowledge_document` — Data source lookup searches published and unpublished knowledge bases by `knowledge_base_name`, matches documents by `title`, and optionally filters by `category_name` when multiple documents share the same title.

#### Apply behavior
None detected.

#### Export behavior
- `genesyscloud_integration_credential` — `name` is no longer exported as a reference to `genesyscloud_integration`; exported values retain the raw `Integration-`-prefixed name.
- `genesyscloud_knowledge_category` — `knowledge_base_name` is resolved from `knowledge_base_id` during export; `name` and `knowledge_base_name` map to data source lookup attributes when exporting as data sources.
- `genesyscloud_knowledge_document` — `knowledge_base_name` is resolved from `knowledge_base_id` during export; `category_name`, `title`, and `knowledge_base_name` map to data source lookup attributes when exporting as data sources.
- `genesyscloud_knowledge_document_variation` — `name` maps to a data source lookup attribute when exporting as data sources.
- `genesyscloud_knowledge_knowledgebase` — `core_language` maps to a data source lookup attribute when exporting as data sources.
- `genesyscloud_knowledge_label` — `knowledge_base_name` is resolved from `knowledge_base_id` during export; `name` and `knowledge_base_name` map to data source lookup attributes when exporting as data sources.
- `genesyscloud_outbound_messagingcampaign` — `whats_app_config.content_template_id` and `whats_app_config.whats_app_integration_id` are resolved as Terraform references during export.
- `genesyscloud_responsemanagement_response` — `library_id` is resolved as a reference to `genesyscloud_responsemanagement_library` during export; `library_id` maps to `library_ids` when exporting as data sources.

### Resources removed
None detected.

### Data sources added
- `genesyscloud_knowledge_document` — Look up a knowledge document by `title` and `knowledge_base_name`, with optional `category_name` to disambiguate documents that share the same title.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing Terraform configurations are likely unaffected unless you manage outbound messaging campaigns or use provider export.
- `genesyscloud_outbound_messagingcampaign` now supports WhatsApp configuration alongside email and SMS; campaigns using only email or SMS remain valid, but you can now choose WhatsApp as the single messaging channel.
- A new `genesyscloud_knowledge_document` data source lets you reference documents by title and knowledge base name instead of managing IDs directly.
- Export output for knowledge resources and `genesyscloud_integration_credential` may differ; re-export if you rely on generated references or data source mappings.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.4/docs) before making upgrade decisions._
