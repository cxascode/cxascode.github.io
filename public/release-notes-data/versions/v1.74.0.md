## CX as Code v1.74.0

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.74.0)

### Resources added
None detected.

### Resources changed

#### genesyscloud_routing_queue

Added:
- `media_settings_message.enable_inactivity_timeout` — enable inactivity timeout for message interactions; defaults to false.
- `media_settings_message.inactivity_timeout_settings` — inactivity timeout configuration block for messages.
- `media_settings_message.inactivity_timeout_settings.timeout_seconds` — timeout in seconds before the inactivity action runs.
- `media_settings_message.inactivity_timeout_settings.action_type` — action on timeout; valid values are `DISCONNECT` or `SEND_TO_ARCHITECT_FLOW`.
- `media_settings_message.inactivity_timeout_settings.flow_id` — architect flow ID when `action_type` is `SEND_TO_ARCHITECT_FLOW`.

Removed:
- None detected.

Changed:
- `media_settings_message` — uses a message-specific block schema; `alerting_timeout_sec` must be at least 7, `service_level_percentage` must be between 0 and 1, and `service_level_duration_ms` must be at least 1000.

#### genesyscloud_task_management_worktype

Added:
- `flow_rules_enabled` — when true, the worktype's flow rules are processed; defaults to false.

Removed:
- None detected.

Changed:
- None detected.

#### genesyscloud_task_management_worktype_status

Added:
- `auto_terminate_workitem` — when true, workitems are automatically terminated when they enter this status (typically used for Closed category statuses).

Removed:
- None detected.

Changed:
- None detected.

#### genesyscloud_team

Added:
- None detected.

Removed:
- None detected.

Changed:
- `member_ids` — teams with no members now return an empty list in state instead of null.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing configurations are generally unaffected unless you use the changed resources above.
- `genesyscloud_routing_queue` gains message inactivity timeout settings under `media_settings_message`; invalid service-level or alerting values may now fail plan validation.
- `genesyscloud_task_management_worktype` and `genesyscloud_task_management_worktype_status` add optional flags for flow-rule processing and automatic workitem termination.
- `genesyscloud_team` may show `member_ids = []` instead of null after refresh when a team has no members.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.74.0/docs) before making upgrade decisions._
