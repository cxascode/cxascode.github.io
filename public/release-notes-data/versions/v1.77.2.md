## CX as Code v1.77.2

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.2)

### Resources added
None detected.

### Resources changed

#### genesyscloud_outbound_contact_list

Added:
- `whats_app_columns` — optional block set for WhatsApp contact columns (`column_name`, `type`). Changing this attribute forces replacement of the contact list. At least one of `phone_columns`, `email_columns`, or `whats_app_columns` must be configured.

Removed:
- None detected.

Changed:
- `phone_columns` — now documented as required when both `email_columns` and `whats_app_columns` are empty.
- `email_columns` — now documented as required when both `phone_columns` and `whats_app_columns` are empty.

#### genesyscloud_flow

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — flow export jobs now include the published flow version when available, targeting the correct published revision.
- Export behavior — flow listing and export operations retry more reliably when the API rate-limits requests or returns transient export job failures.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing `genesyscloud_outbound_contact_list` configurations are likely unaffected unless you add or manage WhatsApp columns.
- Contact lists that use only phone or email columns continue to work; you may optionally configure `whats_app_columns` for WhatsApp outbound dialing.
- Flow export workflows may complete more reliably under API rate limiting, with no schema changes required for `genesyscloud_flow`.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.2/docs) before making upgrade decisions._
