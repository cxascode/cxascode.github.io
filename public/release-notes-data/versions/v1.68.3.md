## CX as Code v1.68.3

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.68.3)

### Resources added
None detected.

### Resources changed
None detected.
### Runtime behavior changes

#### Plan behavior
None detected.

#### Apply behavior
- `genesyscloud_integration_action` — when updating a function data action, the provider now creates or updates the action draft using the current `integration_id`, `name`, `category`, `secure`, contract, and `config` from Terraform state before uploading the function zip from `function_config.file_path`.
- `genesyscloud_integration_action` — retries fetching the function zip ID when it is not immediately available after upload, reducing transient apply failures during function data action updates.
- `genesyscloud_integration_action` — reads the latest draft version from the API before publishing instead of assuming version 1, reducing version-mismatch errors during apply.
- `genesyscloud_integration_action` — function zip uploads set an explicit content length instead of using chunked transfer encoding, improving upload reliability for `function_config.file_path`.

#### Export behavior
None detected.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- `genesyscloud_integration_action` resources that use `function_config` for function data actions should see more reliable update, upload, and publish behavior; no configuration changes are required.
- Integration actions that do not use `function_config` are likely unaffected.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.68.3/docs) before making upgrade decisions._
