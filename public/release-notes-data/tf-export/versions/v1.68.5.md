## CX as Code v1.68.5 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.68.5)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

None detected.

### Related export output changes

- Exported `genesyscloud_knowledge_document` resources can now resolve `label_names` from plain label name strings to Terraform references for matching `genesyscloud_knowledge_label` resources (for example, `${genesyscloud_knowledge_label.<label>.knowledge_label[0].name}`) when those labels are included in the export run. This improves creation-order dependencies so knowledge labels are applied before documents in re-imported configuration.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected. Re-exporting knowledge documents may produce `label_names` values as Terraform references instead of plain label name strings, which can help avoid apply-order issues where documents were created before their labels.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.68.5/docs) before making upgrade decisions._
