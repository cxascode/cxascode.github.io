## CX as Code v1.71.0 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.71.0)

### Resource attributes added

- `max_concurrent_threads` — Optional integer (default `10`) that sets the maximum number of concurrent threads used during export. This is separate from the provider token pool size. Changing this value requires replacing the `genesyscloud_tf_export` resource.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- Resource config map building during export now runs concurrently, limited by `max_concurrent_threads`.
- If `max_concurrent_threads` is unset or set to `0`, export concurrency defaults to `10` (matching the previous fixed concurrency).

### Related export output changes

None detected.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected; default export concurrency remains 10. You can optionally set `max_concurrent_threads` to tune export speed versus API load. Changing `max_concurrent_threads` forces replacement of the export resource.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.71.0/docs) before making upgrade decisions._
