## CX as Code v1.80.0 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.80.0)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- Exported `terraform.tfstate` entries now include `"schema_version": 0` in each resource's `meta` block when the exported block has a type prefix (for example, `resource` or `data`).

### Related export output changes

None detected.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected; no resource schema attributes were added, removed, or changed. Re-exported state files may differ only by the added `schema_version` field in resource `meta` entries.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.80.0/docs) before making upgrade decisions._
