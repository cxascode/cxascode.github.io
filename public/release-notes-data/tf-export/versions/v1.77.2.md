## CX as Code v1.77.2 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.2)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

- `max_concurrent_threads` — The value in your `genesyscloud_tf_export` configuration is now used for export concurrency. Provider `max_clients` is applied only when `max_concurrent_threads` is zero or less.

### Export behavior

None detected.

### Related export output changes

None detected.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations that set `max_concurrent_threads` now run with that value instead of being overridden by provider `max_clients`. Configurations that omit `max_concurrent_threads` use the resource default rather than provider `max_clients`; set `max_concurrent_threads` explicitly, or set it to `0` to fall back to provider `max_clients`.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.2/docs) before making upgrade decisions._
