## CX as Code v1.70.0 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.70.0)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- When `use_legacy_architect_flow_exporter` is `false`, the export pipeline now switches to the newer architect flow exporter after the initial flow export step and carries forward sanitized flow data from that step. Exported architect flow configurations may be more complete than in v1.69.3, where that sanitized data was not preserved when the exporter was replaced during per-resource processing.

### Related export output changes

- Exported `genesyscloud_architect_flow` blocks may include additional or more complete configuration when not using the legacy architect flow exporter, because sanitized flow data is retained across the exporter handoff.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected and do not require changes. If you export architect flows with `use_legacy_architect_flow_exporter` set to `false`, re-run and diff exports against v1.69.3 output; generated flow configuration may differ where previously dropped sanitized values are now included.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.70.0/docs) before making upgrade decisions._
