## CX as Code v1.73.0 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.73.0)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- Flow dependency resolution and `depends_on` generation are more reliable when `add_depends_on` is enabled, particularly during concurrent export processing.
- Dependency lists are merged and written into exported configuration only when valid dependency metadata is returned.
- When `TF_LOG` is enabled, dependency resolution and `depends_on` generation emit additional diagnostic messages, including warnings for invalid or unresolved dependency references.

### Related export output changes

- Exported managed resources that participate in flow dependency resolution may include more complete or consistent `depends_on` blocks.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected. Exports with `add_depends_on` enabled may produce more reliable `depends_on` output; re-export if prior runs produced incomplete dependency references.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.73.0/docs) before making upgrade decisions._
