## CX as Code v1.77.4 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.4)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- Permission errors logged while listing resources during export are now recorded more reliably when multiple resource types are processed concurrently, improving consistency of the end-of-run error summary.
- Export sanitization now invokes custom attribute resolvers that require provider client configuration, which may update exported attribute values where those resolvers are defined.

### Related export output changes

- Exported string attributes that reference other resources and use ID transformation during resolution may now have the entire attribute value resolved as a Terraform reference, rather than preserving surrounding text while substituting only the embedded identifier portion.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected; no resource schema changes were detected. Re-run exports and review generated HCL if you depend on partial identifier substitution within larger string values, or if you export resources that use custom attribute resolvers requiring provider client configuration—output may differ from v1.77.3.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.4/docs) before making upgrade decisions._
