## CX as Code v1.77.3 — genesyscloud_tf_export

[View provider release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.3)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- `enable_dependency_resolution` — When using include_filter_resources or include_filter_resources_by_id, enable_dependency_resolution now controls depends_on generation without requiring a non-empty resource-type filter list.
- `enable_dependency_resolution` — When using exclude_filter_resources or the default export mode, enable_dependency_resolution no longer adds depends_on blocks even when set to true.
- Failures in custom file writers for exported resources are reported as errors instead of warnings, causing the export to fail.
- Permission errors encountered while listing resources during export are collected and reported at the end of the export run.

### Related export output changes

None detected.

### Upgrade impact

- Existing Terraform resource configurations are likely unaffected; changes primarily affect `genesyscloud_tf_export` output and export error handling.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.3/docs) before making upgrade decisions._
