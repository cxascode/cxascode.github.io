## CX as Code v1.78.0 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.78.0)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

- `replace_with_datasource` — Description updated to clarify that a resource type alone (equivalent to `type::`) is a valid match pattern, in addition to `resource_type::regular expression`.

### Resource behavior

- `exclude_attributes` — Invalid entries (for example, a value with no attribute path after the resource type) are skipped with a warning instead of failing the export.
- `exclude_attributes` — Exclusions are applied in a final pass before output, so entries still apply to resource types discovered during dependency resolution (`enable_dependency_resolution = true`).
- `exclude_attributes` — Nested attribute paths (for example, `resource_type.nested.attribute`) are removed from exported configuration maps.
- `replace_with_datasource` — A list entry that is only a resource type name (without `::` and label) now matches all exported instances of that type.

### Export behavior

- Per-resource JSON export files are written as `data_<resource_type>.json` instead of `<resource_type>.json`.

### Related export output changes

None detected.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected; no resource schema attributes were added or removed. Re-run exports if you use `exclude_attributes` or `replace_with_datasource`, because exclusion handling and datasource replacement matching may produce different output. Update any automation that reads per-resource JSON files to expect the `data_` filename prefix.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.78.0/docs) before making upgrade decisions._
