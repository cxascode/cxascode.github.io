## CX as Code v1.78.0 — genesyscloud_tf_export

[View provider release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.78.0)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

- `exclude_attributes` — Invalid entries are skipped with a warning instead of failing the export; exclusions are applied in a final pass so they work for resource types discovered during dependency resolution.
- `replace_with_datasource` — Accepts a resource type without a label suffix (equivalent to type::) to replace all exported instances of that type with data sources.

### Export behavior

- Per-resource JSON files are named data_<resource_type>.json instead of <resource_type>.json.

### Related export output changes

None detected.

### Upgrade impact

- Users running `genesyscloud_tf_export` may see different exported HCL for singleton resources, flows, integration actions, contact lists, and web deployment resources; JSON export filenames change to the `data_` prefix; `exclude_attributes` and `replace_with_datasource` behave more reliably.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.78.0/docs) before making upgrade decisions._
