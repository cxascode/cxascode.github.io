## CX as Code v1.68.1 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.68.1)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- Export continues when resource map building produces warnings only; those warnings are returned with the export result instead of stopping map retrieval early.
- When the export filter matches no valid resource types, the resulting error may include warnings collected during map building rather than only a single no-valid-resource-types message.
- The message `Building sanitized resource maps` is written to standard console output during export.

### Related export output changes

- Exported flow configurations may differ because per-attribute filepath variable resolution during sanitization was removed.
- Flow exports no longer apply a post-write instance state attribute update that previously ran during flow export.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are unchanged and should not require updates. Re-run and diff exports that include flow resources, because flow output may change. Exports that previously stopped on non-fatal map-building warnings may now proceed and surface those warnings in the Terraform result.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.68.1/docs) before making upgrade decisions._
