## CX as Code v1.68.6 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.68.6)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- During export sanitization, attribute values that reference the same resource's own ID are removed from the generated Terraform configuration instead of being written to the export output.

### Related export output changes

- Exported resource blocks may omit attributes whose value matched that resource's own ID, so re-exported `.tf` files can differ from output produced by v1.68.5 when those self-referential values were previously included.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are unchanged and should not require updates. Re-run and diff exports if you rely on previously exported attribute values that pointed back to the same resource ID, because those attributes may no longer appear in generated configuration.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.68.6/docs) before making upgrade decisions._
