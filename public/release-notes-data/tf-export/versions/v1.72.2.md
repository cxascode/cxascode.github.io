## CX as Code v1.72.2 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.72.2)

### Resource attributes added

- `include_filter_resources_by_id` — Optional list of `{resourceType}::{resourceId}` values to export only specific resources by ID. See the export guide for supported resource types and format details. Changing this attribute forces a new export run.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

- Filter selection attributes `resource_types`, `include_filter_resources`, `exclude_filter_resources`, and `include_filter_resources_by_id` are mutually exclusive.

### Export behavior

- When `include_filter_resources_by_id` is set, the export includes only resources whose type and ID match one of the listed `{resourceType}::{resourceId}` values.

### Related export output changes

None detected.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected and do not require changes. You can optionally set `include_filter_resources_by_id` to export specific resources by ID instead of using type or regex filters. Changing any filter attribute forces a new export run.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.72.2/docs) before making upgrade decisions._
