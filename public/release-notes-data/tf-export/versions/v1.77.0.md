## CX as Code v1.77.0 — genesyscloud_tf_export

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.77.0)

### Resource attributes added

None detected.

### Resource attributes removed

None detected.

### Resource attributes changed

None detected.

### Resource behavior

None detected.

### Export behavior

- During dependency resolution, flows exported as data sources (`data.genesyscloud_flow`) are skipped for dependent resource discovery because they are referenced only and not managed as resources.
- During configuration sanitization, reference attributes backed by custom resolvers that can infer a target type from the exported configuration are now converted to Terraform resource or data source references when that type can be determined.

### Related export output changes

- `genesyscloud_flow` — Exports that include data source flow blocks may produce fewer related resources and different `depends_on` entries, because dependency fetching is no longer run for those flows.

### Upgrade impact

Existing `genesyscloud_tf_export` configurations are likely unaffected. Re-run and diff exports if you export flows as data sources or rely on dependency resolution pulling in flow-related resources; output may be leaner for referenced-only flows. Some reference attributes may now appear as Terraform references instead of raw GUIDs where dynamic reference-type resolution applies.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.77.0/docs) before making upgrade decisions._
