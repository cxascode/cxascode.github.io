<!-- site-updates:auto -->
## Site updates — June 8, 2026

Richer permalinks and a dedicated export release-notes view.

### Version-aware permalinks

- Shareable URLs include the selected provider version (e.g. `/genesyscloud_queue/v1.83.0`) so links open the same dependency tree and templates.

### Export release notes

- Release notes dialog gained a **Provider / Export** scope toggle.
- `genesyscloud_tf_export` changes are shown separately from the rest of the provider changelog.
