## v1.68.3

### Resources added
None detected.

### Resources changed

#### genesyscloud_integration_action

Added:
- None detected.

Removed:
- None detected.

Changed:
- Update behavior — when updating a function-type integration action, creates or updates the action draft with current `integration_id`, `name`, `category`, `secure`, contract, and `config` before uploading the function zip from `function_config.file_path`.
- Apply behavior — retries when the function zip ID is not immediately available after upload, reducing transient apply failures.
- Apply behavior — retrieves the latest draft version before publishing, reducing version-mismatch errors during apply.
- `function_config.file_path` — function zip uploads send an explicit content length instead of chunked transfer encoding, improving upload reliability.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- `genesyscloud_integration_action` resources that use `function_config` for function-type actions should see more reliable update, upload, and publish behavior; no configuration changes are required.
- Integration actions that do not use `function_config` are likely unaffected.
