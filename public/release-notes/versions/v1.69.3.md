## CX as Code v1.69.3

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.69.3)
### Resources added
None detected.

### Resources changed

#### genesyscloud_flow

Added:
- None detected.

Removed:
- None detected.

Changed:
- Apply behavior — flow publish polling handles incomplete Architect job responses (missing job status, tracing message text, or flow ID) without failing with a provider error; failed publishes may omit tracing messages that have no text.

#### genesyscloud_group

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — exported configurations may omit empty `addresses` blocks again instead of preserving explicit empty arrays; empty `owner_ids` arrays are still preserved during export.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing configurations are likely unaffected.
- `genesyscloud_flow` apply operations are more resilient when Architect publish jobs return incomplete status data; review apply logs if a publish succeeds but state is not updated as expected.
- Re-exporting `genesyscloud_group` resources may omit empty `addresses` blocks where v1.68.6–v1.69.2 export output included them explicitly.
- Shared retry helpers only automatically retry on state-wait timeout errors; other timeout errors may no longer trigger an automatic retry across resources that use the shared retry logic.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.69.3/docs) before making upgrade decisions._
