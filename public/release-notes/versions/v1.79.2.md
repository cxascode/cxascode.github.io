## CX as Code v1.79.2

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.79.2)
### Resources added
None detected.

### Resources changed

#### genesyscloud_business_rules_decision_table

Added:
- `timeouts.read` — Optional read timeout; defaults to 8 minutes.

Removed:
- None detected.

Changed:
- Read timeout behavior — Read operations now honor the resource `timeouts` block instead of a fixed 1-minute internal limit.

#### genesyscloud_outbound_contact_list

Added:
- None detected.

Removed:
- None detected.

Changed:
- Contact list CSV export — Column headers are normalized by stripping invisible Unicode characters and extra quotes before system columns are removed, preventing mismatched or malformed headers such as `inin-outbound-id`.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing Terraform configurations are likely unaffected.
- `genesyscloud_business_rules_decision_table` reads may take longer before timing out by default (8 minutes instead of 1 minute); override with a `timeouts { read = "..." }` block if needed.
- Users exporting outbound contact lists with CSV files that had BOM-prefixed or over-quoted column headers should see more reliable column matching during export.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.79.2/docs) before making upgrade decisions._
