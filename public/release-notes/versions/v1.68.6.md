## CX as Code v1.68.6

[View release changelog](https://github.com/MyPureCloud/terraform-provider-genesyscloud/releases/tag/v1.68.6)
### Resources added
None detected.

### Resources changed

#### genesyscloud_group

Added:
- None detected.

Removed:
- None detected.

Changed:
- `addresses` — setting an empty list (`[]`) now clears existing group addresses on update instead of leaving them unchanged.
- Export behavior — exported configurations preserve empty `addresses` and `owner_ids` arrays instead of omitting them.

#### genesyscloud_journey_segment

Added:
- None detected.

Removed:
- None detected.

Changed:
- `assignment_expiration_days` — changes to this attribute are now applied during resource updates.

#### genesyscloud_knowledge_document_variation

Added:
- None detected.

Removed:
- None detected.

Changed:
- `document_version` — no longer settable in configuration; now computed only (was previously optional).

#### genesyscloud_routing_queue

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — `direct_routing.backup_queue_id` is omitted from export output when its value matches the queue's own ID.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing configurations are likely unaffected for most resources.
- `genesyscloud_group` resources can now remove all addresses by setting `addresses = []`; previously an empty list did not clear addresses in Genesys Cloud.
- Remove `document_version` from `genesyscloud_knowledge_document_variation` configurations if it is set as an input argument; plans may fail because the attribute is now computed only.
- `genesyscloud_journey_segment` resources that update `assignment_expiration_days` should now see those changes applied correctly.
- Export output for `genesyscloud_group` may include explicit empty `addresses` or `owner_ids` blocks where they were previously omitted.
- Export output for `genesyscloud_routing_queue` may omit `direct_routing.backup_queue_id` when it references the queue itself.

---

_Generated by AI from source-code diffs. Review against [provider documentation](https://registry.terraform.io/providers/MyPureCloud/genesyscloud/1.68.6/docs) before making upgrade decisions._
