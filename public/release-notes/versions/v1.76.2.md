## v1.76.2

### Resources added
None detected.

### Resources changed

#### genesyscloud_flow

Added:
- None detected.

Removed:
- None detected.

Changed:
- Export behavior — dependency pagination metadata is handled safely; export no longer fails when the consumed-resources API response omits `PageCount`.
- Export behavior — consumed resource dependencies missing a name are skipped during export dependency resolution.

### Resources removed
None detected.

### Data sources added
None detected.

### Data sources changed
None detected.

### Data sources removed
None detected.

### Provider configuration changes
None detected.

### Upgrade impact
- Existing Terraform configurations are likely unaffected; changes refine export-time dependency resolution for flows.
- Flow exports may omit dependencies for consumed resources with incomplete metadata (missing name).
- Exports that previously failed when dependency pagination metadata was absent may now complete successfully.
